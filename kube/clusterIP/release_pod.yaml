apiVersion: v1
kind: Pod
metadata:
  name: release-nginx
  labels:
    app: release-nginx
spec:
  containers:
  - name: nginx
    image: skhaneef19/cloudethix_release_nginx_haneef:v1
    ports:
    - containerPort: 80
